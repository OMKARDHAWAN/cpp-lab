#include <iostream>
using namespace std;

int main()
{
    int n;

    int array[] = {50, 30, 40, 20, 10};
    int len = sizeof(array) / sizeof(array[0]);
    cout << len << endl;
   

    return 0;
}